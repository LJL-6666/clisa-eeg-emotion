# DE + SVM 基线运行环境（纯 CPU，无需 GPU）。
# 版本与「产出本仓库结果的环境」完全一致，便于复现。
# Python 3.10.4
#
# 重建方式（任选其一）：
#   conda create -n emotion_test python=3.10.4 -y && conda activate emotion_test && pip install -r requirements.txt
#   或在任意 python=3.10 环境下：pip install -r requirements.txt
#
# 说明：torch 仅被 import（个别脚本引入），实际计算未用到 GPU，CPU 版即可。

numpy==1.22.3
scipy==1.7.3
scikit-learn==1.1.1
mne==1.7.1
hdf5storage==0.1.19
h5py==3.11.0
pandas==1.4.3
joblib==1.1.0
matplotlib==3.5.2
torch==1.12.1
